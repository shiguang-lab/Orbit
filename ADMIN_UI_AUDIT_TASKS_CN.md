# 管理端交互与显示审查任务清单

## 第 1 轮：结构与基础布局（已完成）

- [x] 顶部 Header 在窄屏下缩小间距并允许操作项换行，避免横向溢出。
- [x] 移动端语言选择器隐藏冗余文字，仅保留旗帜与下拉箭头。
- [x] Toast 改为视口自适应宽度，长 URL/错误信息自动断行。
- [x] 面包屑允许换行，统一使用主题文字变量。
- [x] 空状态、列设置、筛选栏移除暗色硬编码，修复浅色主题对比度。

## 第 2 轮：操作与响应式（已完成）

- [x] 侧栏导航项提高触控高度，分组支持 Enter/Space 键盘展开收起。
- [x] 侧栏分组补充 `aria-controls`，固定按钮补充可读标签。
- [x] 移动端 RTL 侧栏改为从正确方向滑入/滑出。
- [x] 提供商卡片底部状态允许换行，避免两列窄卡片截断。
- [x] 筛选/列设置浮层限制为视口宽度，避免超出屏幕。
- [x] 首页更新提示在移动端改为纵向布局，按钮不再挤压正文。

## 第 3 轮：主题一致性与回归（已完成）

- [x] 日志导出菜单、清理状态统一使用主题 surface/border/text 变量。
- [x] 健康页能力卡片统一使用主题文字颜色。
- [x] 配额燃烧图 Tooltip 改用主题 surface/border 变量。
- [x] 检查改动文件格式与 diff 空白，补跑侧栏 UI 测试。

## 第 4 轮：线上路由与移动端（已完成）

- [x] 线上巡检首页、提供商、组合、分析、日志、设置、成本、代理技能、MCP、A2A 等核心页面。
- [x] 复核 375px 页面无根节点横向溢出；分析/日志/成本宽表均有可滚动容器。
- [x] 验证移动侧栏打开、遮罩点击关闭，确认触控路径可用。
- [x] 成本图表坐标轴改用 `--color-text-muted`，修复主题切换下潜在隐形文字。

## 第 5 轮：交互可达性与全路由（已完成）

- [x] 扩大巡检至活动、批处理、缓存、CLI、会话、审计、插件、运行时等路由。
- [x] 顶部移动菜单按钮补充 40px 触控区域和可访问名称，避免小目标/无标签。
- [x] 禁止访问页移除旧主题变量，统一使用主题 token。
- [x] 记录未注册的直接路径（如 `/dashboard/gamification`、`/dashboard/resilience`、`/dashboard/system`）为路由核对项；当前未发现核心侧栏入口指向这些路径。

## 待后续专项任务

- [x] 在真实浏览器完成 375/1440px 截图巡检并记录逐页差异；320/768/1024 可作为后续专项回归尺寸。
- [x] 清理管理端范围内残留的 `var(--text-*)` 旧主题变量。
- [ ] 对提供商、日志、设置、分析四类高频页面补充移动端交互回归用例。

## 截图问题追加五轮复审（本次完成）

### 第 1 轮：顶部区域

- [x] 删除非 Electron 环境下无功能的三色装饰灯。
- [x] 将侧栏折叠按钮移入品牌栏，避免独占一行。
- [x] 折叠态按钮固定在右上角，品牌图标保持居中。

### 第 2 轮：分组控件

- [x] 分组标题字号提升至可读尺寸，取消强制全大写。
- [x] 折叠箭头改为独立按钮，移除嵌套按钮语义问题。
- [x] 图钉按钮保持可见并扩大触控区域。

### 第 3 轮：菜单排版

- [x] 侧栏展开宽度提升并限制为 `min(88vw, 260px)`。
- [x] 菜单图标、行高、辅助说明统一增大并改善对齐。
- [x] 搜索框和版本号层级调整，减少小字号堆叠。

### 第 4 轮：底部与移动端

- [x] 重启/停止按钮取消文字截断，补充可访问名称。
- [x] 同步状态控件提升高度和字号，保持底部对齐。
- [x] 移动抽屉沿用视口宽度上限，避免窄屏溢出。

### 第 5 轮：回归检查

- [x] 复核桌面、移动、RTL 侧栏结构及键盘操作路径。
- [x] 检查分组展开、固定、当前项高亮和滚动区域无回归。
- [x] 完成 Prettier、diff 空白和 Dashboard 类型基线检查。

## 品牌、上传与静态资源专项（本轮完成）

- [x] 默认应用名称统一为“智枢”，修复旧名称在页面切换时短暂闪现。
- [x] 登录页、落地页、侧栏、加载页、日志、拓扑和文档侧栏统一使用智枢 Logo。
- [x] 接入指定 Logo 图片并生成 favicon、PWA 192/512 图标及 Apple Touch 图标。
- [x] Logo 上传改为即时本地预览、持久化成功后同步侧栏，并清除冲突 URL。
- [x] 放宽 Logo Base64 校验上限，修复正常 500KB 图片上传被接口拒绝。
- [x] 补齐 `/screenshots/dashboard.png`，修复 PWA 清单截图 404；线上需下次部署后生效。
- [x] 连接账号行拆分信息区与操作区，修复状态徽章、错误文本和操作按钮互相挤压。
- [x] 连接账号行在窄屏自动堆叠，避免横向溢出和操作区垂直错位。
- [x] 账号优先级上下移动按钮补齐可访问名称并扩大点击区域。
- [x] Dario 账号列表采用同类响应式排列，长账号名不再挤压操作按钮。
